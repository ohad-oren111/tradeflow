# tradeflow watchdog — host-side venv deps (NOT container deps)
# Installed into ~/tradeflow/.watchdog-venv/ by scripts/install_watchdog.sh.
# Ranges mirror requirements.txt convention; PyPI names (dash, not underscore).
# Do NOT add these to requirements.txt or pyproject.toml — those are container manifests.
ib-async>=1.0.3,<2.0
httpx>=0.27,<0.29
python-dotenv>=1.0,<2.0
